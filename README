# Skip Hire Service Redesign

A modern, responsive React application for booking skip hire services. Features a beautiful UI with real-time pricing data.

## Quick Start

### Prerequisites
- Node.js 14+
- Yarn

### Installation

1. **Clone the project**
   ```bash
   git clone https://github.com/BereketRetta/remw-challange.git
   cd skip-hire-service
   ```

2. **Install dependencies**
   ```bash
   yarn install
   ```

3. **Run the application**
   ```bash
   yarn start
   ```

The app will open at `http://localhost:3000`

## Scripts

- `yarn start` - Run development server
- `yarn build` - Build for production
- `yarn test` - Run tests
- `yarn eject` - Eject from Create React App

## Dependencies

```json
{
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "lucide-react": "^0.263.1"
}
```

## Features

- Modern, clean design
- Mobile responsive
- Real-time API data
- Interactive skip selection

## Project Structure

```
├── src/
│   ├── App.js          # Main component
│   └── index.js        # Entry point
├── public/
│   └── index.html      
├── package.json
└── README.md
```